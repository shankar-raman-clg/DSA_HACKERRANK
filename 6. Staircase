#include <stdio.h>
#include <stdlib.h>

void staircase(int n) {
    for (int i = 1; i <= n; i++) {
        // Print spaces
        for (int j = 0; j < n - i; j++) printf(" ");
        // Print hashes
        for (int k = 0; k < i; k++) printf("#");
        // New line
        printf("\n");
    }
}
